@model IEnumerable<Sistema.laboratorio.Medico>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MedicoNombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MedicoMatricula)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MedicoNombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MedicoMatricula)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MedicoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MedicoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MedicoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
